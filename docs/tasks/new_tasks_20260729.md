# New Tasks （2026-07-29 created)

[x] in top right the user page, the "Profile" is not implemented yet. Please implemenat the function
  * add a new page for user to modify/save
  * The info  for modifying should match the database scheme

[x] In "Setting" page, instead of put the Categories/Event Types in one page, I want to add an extra layer, i.e. in Setting page only contains 2 link entries as "Categories", "Event Types" for click, when click each link then the associated setting page show up. 
  * new pages for "Categories", "Event Types" should be created
  * 2 link entries should replace current 2 tabels 

[x] Add a new table called "tenant", with basic fields: "institution_name", "description","address","contact","phone","email","website","social_media"
  * refer to "tenant_concept.md" for other functions

[x] Add a new fields called called "image" that can let user upload image with limited size, like 2M

[x] in Ojbect table, Add a new field called "extra" with json type that will contain customized field. 
  * Add a new link in "Setting" which will prop a page that allow user to create new customized scheme
  * The new customized page is associated with the "tenant",each tenent will have 1 customized schema 
  * The user can add new field names , add a "note" or "comment" just for record,  and save
  * When add new object, extra fields from the cusomized schema will appended under the normal fields, the use will populate them, the data will converted into json and filled into the "extra" field


[x] Create another "object info" page that:
  * Display the object information, including which instituion it belong to, owner
  * For description, only first 100 characters if more than 100 
  * All the extra fields will be displayed after the normal fields
  * Add a setting of "Show Object Info Without Authentication" as check box with default checked/yes
  * If above setting is checked/yes, the page can be viewed without authentication

[x] The link/share of the 2D barcode along with the 2D barcode itself should be the object info page
  * For now just this page. In future the content has different content based on  authenticated or not
