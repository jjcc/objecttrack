# ObjectTrack Web — Found Issues

Reviewed: 2026-07-23（初次版）, 2026-07-29 由 Codex 复核并实施修复

Scope:
- Next.js web application in `/home/jchen/workspaces/objecttrack`
- Local Supabase migrations and generated database types
- Compatibility with the Flutter mobile application in
  `/home/jchen/workspaces/objtrack_mobil`
- Compatibility with live Supabase project `hdpfxqqvrpmelcregemq`

---

## 已完成修复

### P0. 迁移文件名时间戳与 live migration history 不一致
- 已重命名 3 个 migration 文件，使其与 live 已登记历史一致：
  - `20260723161741_fix_transfer_critical_workflow.sql`
  - `20260723162036_harden_transfer_schema.sql`
  - `20260723162348_restrict_transfer_direct_writes.sql`

### P1. `transfer_requests.id` identity 生成策略不一致
- 新增 migration：`20260729001241_align_transfer_requests_id_identity_to_by_default.sql`
- 以 live 的 `GENERATED BY DEFAULT AS IDENTITY` 为准，仅在仍为 ALWAYS 时改写

### P2. Reject 原因未传递
- transfers 列表页新增拒绝理由 Modal，支持输入 `reason`
- `rejectTransfer` 已传入 `p_reason`，审计事件可记录原因

### P3. 前端类型仍有 `any` / 弱类型
- 重新生成 Supabase TypeScript types
- 替换 transfer 页面/helper 里的 `as any` / `Record<string, unknown>`
- 状态已改为 `TransferStatus` 枚举

### P4. 前端列表为两段读
- 新增 migration：`20260729010000_add_transfer_requests_display_view.sql`
- 新建只读 view `transfer_requests_display`，集中返回 request + object + from/to 名字
- transfers 列表和详情改为单段读，去掉 `profile_names` 两段查询

---

## 当前状态

- `npm run build` 通过
- transfers 审批写入为原子 RPC，前端读模型已收紧
- 无已知 critical/high 未解决 issue
- 剩余为体验增强或通知系统
