# ObjectTrack Web — Found Issues

Reviewed: 2026-07-23（初次版）, 2026-07-29 由 Codex 复核更新

Scope:
- Next.js web application in `/home/jchen/workspaces/objecttrack`
- Local Supabase migrations and generated database types
- Compatibility with the Flutter mobile application in
  `/home/jchen/workspaces/objtrack_mobil`
- Compatibility with live Supabase project `hdpfxqqvrpmelcregemq`

## 复核结论

以下问题已由后续提交修复，不再成立：
- transfers 查询已改用 `profile_names()` RPC，原 PostgREST 关系名问题已消除
- approve/reject 已改为数据库原子 RPC，非原子写入问题已消除
- 直写 transfer_requests 的权限已撤销，强制走 RPC，避免绕过审计
- 审计事件写入与状态更新同事务，不会静默跳过

**直接验证结果**：经 Codex 对 live schema 与本地 migrations 逐项比对，当前表、列、RLS、policy、RPC 均一致；未见缺失 schema。

**schema drift**：仅发现轻微历史版本漂移与一处 identity 差异，见下方 P0/P1。

---

## 保留 / 新问题

### P0. 迁移文件名时间戳与 live migration history 不一致
**状态**：低危，但会导致后续 `db push` / migration history 校验不一致

本地文件：
- `20260723161136_fix_transfer_critical_workflow.sql`
- `20260723161937_harden_transfer_schema.sql`
- `20260723162330_restrict_transfer_direct_writes.sql`

Live 已登记：
- `20260723161741_fix_transfer_critical_workflow.sql`
- `20260723162036_harden_transfer_schema.sql`
- `20260723162348_restrict_transfer_direct_writes.sql`

**建议**：统一以 live 历史为准重命名本地文件，避免重复执行或 history mismatch。

---

### P1. `transfer_requests.id` identity 生成策略不一致
**状态**：低危，功能正常，但重建数据库时行为可能不同

- 本地 migration：`GENERATED ALWAYS AS IDENTITY`
- Live：`GENERATED BY DEFAULT AS IDENTITY`

**建议**：选定一种策略并同步到本地与 live，例如若应用层不显式插入 ID，统一改为 `ALWAYS`。

---

### P2. Reject 原因未传递
**状态**：功能缺失，不影响一致性，但 audit metadata 信息空洞

当前 `rejectTransfer(supabase, requestId)` 未传 `reason`，DB 中 `reason` 与 audit extra 通常为空。

**建议**：在 transfers UI 增加输入框，`reject_transfer` 传入 `p_reason`。

---

### P3. 前端类型仍有 `any` / 弱类型
**状态**：质量债，不影响运行，但掩盖 schema 变化

`src/types/database.ts` 与页面里存在 `as any`、`Record<string, unknown>` 用法，TS 不强制约束 `TransferRecord.status` 等字段。

**建议**：再生 types 后逐步收紧。

---

### P4. 前端列表为两段读，展示可能短暂不一致
**状态**：体验问题，不是正确性问题

transfers 列表先读 `transfer_requests + objects`，再 RPC 查 `profile_names`。并发场景下 profile 可能为空或延迟更新。

**建议**：新增只读 view / RPC，返回 transfer + object + profile display fields，替代两段读。

---

## 建议修复顺序

1. **P0**：对齐 migration 文件名到 live history
2. **P1**：对齐 `transfer_requests.id` identity 策略
3. **P2**：前端 reject UI 传 reason
4. **P3**：regenerate Supabase TS types
5. **P4**：新增合并读 RPC/view，简化前端查询
