# New Tasks （2026-07-31 created)

Reference: `docs/tenant_management_design.md`

## Phase 1: Tenant boundaries and authorization

Status (2026-07-31): Complete. Roles, centralized authorization, tenant
ownership constraints, RLS policies, migration, documentation, tenant-aware
seed data, and rollback-only verification scripts are implemented. TypeScript,
the production build, a clean local migration reset and seed, both database
verification suites, database lint, advisors, formatting, and static security
review pass. The local Supabase stack and rootless Docker were stopped after
verification; no remote database was modified.

[x] Define the tenant-management roles and permissions
  * Separate regular tenant users, tenant administrators, and internal platform operators
  * Add explicit permissions for tenant settings, invitations, user roles, data updates, reports, tenant creation, and tenant suspension
  * Deny cross-tenant access by default
  * Document which roles receive each permission

[x] Centralize tenant context and authorization in the backend
  * Derive the active tenant from the authenticated user's tenant membership
  * Do not trust a `tenant_id` submitted by the browser without verifying membership and permission
  * Apply tenant scoping to all reads, creates, updates, and deletes of tenant-owned data
  * Return a consistent forbidden response when a user attempts an unauthorized action

[x] Review and complete tenant ownership in the database schema
  * Ensure every tenant-owned record has a `tenant_id` or an unambiguous relationship to a tenant-owned parent
  * Add required indexes, foreign keys, and uniqueness constraints for tenant-scoped queries
  * Prepare a safe migration and backfill for existing records
  * Add tests that prove data from one tenant cannot be accessed or modified by another tenant

## Phase 2: Tenant provisioning and platform operations

Status (2026-07-31): Complete. Transactional provisioning, versioned defaults,
the protected `/ops` control plane, initial-owner work queueing, and platform
audit events are implemented. Clean migration reset, Phase 1 regressions,
Phase 2 rollback verification, database lint/advisors, TypeScript, and the
production build pass. No remote database was modified.

[x] Implement a transactional tenant-provisioning service
  * Create a tenant with its basic information and initial status
  * Apply versioned default settings
  * Create the initial tenant-owner membership or invitation
  * Record an audit event for the operation
  * Roll back required provisioning steps if any required step fails
  * Queue non-critical work, such as sending email, only after the transaction succeeds

[x] Create the internal platform-operations tenant management interface
  * Keep it separate from normal tenant-facing navigation
  * Restrict access to authorized platform operators
  * Add tenant listing, search, details, creation, and editing
  * Add tenant activation and suspension with confirmation and a required reason
  * Do not expose unrestricted cross-tenant editing through tenant-admin routes

[x] Add versioned tenant default settings
  * Store the default-settings version applied to each tenant
  * Apply current defaults only when provisioning a new tenant
  * Provide an explicit migration path when existing tenants need new defaults
  * Test that changing defaults does not silently modify existing tenants

## Phase 3: Tenant administration

Status (2026-07-31): Complete. The protected `/admin` area, audited tenant
profile/settings service, scoped member listing, safe role changes, removals, and
last-owner enforcement are implemented. Clean migration reset, Phase 1-3
rollback suites, database lint/advisors, generated types, TypeScript, and the
production build pass. No remote database was modified.

[x] Add a tenant-administration area inside the main application
  * Show it only to tenant owners and administrators
  * Add pages for tenant profile, settings, members, invitations, and reports
  * Ensure every action is limited to the administrator's current tenant
  * Reuse shared backend services instead of duplicating tenant-management logic in the UI

[x] Implement tenant profile and settings management
  * Allow authorized tenant administrators to view and update supported tenant fields
  * Validate all inputs in the backend
  * Prevent tenant administrators from changing platform-only fields such as tenant status or billing ownership
  * Record settings changes in the audit log

[x] Implement tenant member and role management
  * List members of the current tenant only
  * Allow authorized administrators to change supported tenant roles
  * Prevent removal or demotion of the last tenant owner
  * Prevent privilege escalation to platform-operator roles
  * Record role changes and member removals in the audit log

## Phase 4: Tenant invitations

Status (2026-07-31): Complete. Private invitation and delivery-attempt models,
hashed single-use tokens, rate-limited create/resend/revoke workflows, optional
Resend delivery, authenticated acceptance, membership creation, and audit events
are implemented. A clean migration reset, all Phase 1-4 rollback suites, schema
lint, generated types, TypeScript, and the production build pass. No remote
database was modified.

[x] Add the tenant invitation data model
  * Store tenant, invited email, intended role, inviter, expiry, and status timestamps
  * Use a cryptographically random, single-use invitation token and store only its hash
  * Support pending, accepted, expired, and revoked invitation states
  * Prevent duplicate active invitations when appropriate

[x] Implement invitation creation, delivery, revocation, and resend
  * Verify the inviter's tenant permission before creating an invitation
  * Send the invitation link only to the specified email address
  * Perform email delivery asynchronously after the invitation is committed
  * Allow authorized tenant administrators to revoke or resend a pending invitation
  * Rate-limit invitation creation and resend actions
  * Record all invitation actions in the audit log

[x] Implement secure invitation acceptance
  * Allow the recipient to authenticate or register before acceptance
  * Verify the token hash, expiry, invitation status, and email policy
  * Create membership in the tenant stored on the invitation; never accept a tenant choice from the browser
  * Create the membership and mark the invitation accepted in one transaction
  * Reject reused, revoked, expired, or invalid invitations
  * Add success, invalid-link, expired-link, and already-used user experiences

## Phase 5: Reports and exports

Status (2026-07-31): Complete. Tenant-authorized synchronous inventory CSVs,
tenant-bound background jobs, a service-role worker, private report storage,
60-second signed downloads, seven-day retention cleanup, and report audit events
are implemented. Clean migrations, all Phase 1-5 rollback suites, a live local
worker/Storage lifecycle test, schema lint, generated types, TypeScript, and the
production build pass. No remote database was modified.

[x] Add tenant-scoped report generation
  * Authorize the requested report for the current tenant
  * Ensure report queries cannot include another tenant's data
  * Generate small reports synchronously and large reports as background jobs
  * Capture the requesting user and tenant context when a background job is created

[x] Add secure report storage and download
  * Store generated files in tenant-scoped locations
  * Recheck authorization before allowing a download
  * Use expiring download links where appropriate
  * Define report retention and deletion policies
  * Audit sensitive report generation and downloads

## Phase 6: Auditing, security, and verification

Status (2026-07-31): Complete. Append-only scoped audit views, request IDs,
secret-field rejection, AAL2 TOTP enrollment and enforcement for /ops, operator
access logging, operational metrics, structured authorization-denial logs, and
the operations runbook are implemented. A clean migration reset, every Phase
1-6 rollback suite, schema lint, generated types, TypeScript, React review, and
the 38-route production build pass. No remote database was modified.

[x] Implement an append-only audit log for sensitive tenant actions
  * Record actor, tenant, action, target, timestamp, request or correlation ID, and relevant change metadata
  * Include tenant creation, suspension, settings changes, invitations, role changes, exports, and platform-operator access
  * Restrict audit-log viewing to authorized roles
  * Do not store invitation tokens, passwords, or other secrets in audit metadata

[x] Strengthen platform-operator authentication and access controls
  * Require stronger authentication for the internal operations interface
  * Apply least-privilege permissions to platform-operator accounts
  * Protect platform routes independently from normal tenant-admin routes
  * Log operator access and all cross-tenant actions

[x] Add end-to-end tenant isolation and authorization tests
  * Test regular-user, tenant-admin, tenant-owner, and platform-operator permissions
  * Attempt cross-tenant reads and writes for every tenant-owned resource
  * Test forged or modified `tenant_id` values from clients
  * Test invitation replay, expiry, revocation, and tenant tampering
  * Test report generation and download authorization across tenants

[x] Add operational monitoring and documentation
  * Monitor failed authorization attempts, invitation delivery failures, provisioning failures, and report-job failures
  * Document tenant provisioning, tenant suspension, invitation support, and audit review procedures
  * Document how default-setting migrations are applied
  * Record the criteria for moving the operations control plane to a separate deployment

## Production rollout

Status (2026-07-31): Steps 5-8 are complete. The full migration chain and seven
rollback verification suites passed against the disposable ObjectTrack staging
project. The ObjectTrack2 production backup was verified, the six Phase 1-6
migrations passed a production dry-run, were applied successfully, and the
remote database is up to date. Vercel production deployment
`dpl_7k6y36yjoRdwCoF9md5WSryX8Qxb` reached READY and its public, login, MFA, and
protected operations routes passed HTTP smoke checks. The production
platform-operator grant is active for `bpzmik@163.com`; its TOTP factor is
verified and the AAL2 authorization check passed. A React Server Component
manifest error discovered on the first authenticated `/ops` render was fixed
by moving Mantine compound tables behind client boundaries. Hotfix deployment
`dpl_5b1jmFSJhv16GXbBwS782ZP44Lns` reached READY. The same boundary issue was
then found and fixed on the tenant-admin Audit table; repository-wide scanning
confirmed no remaining server-rendered Mantine compound tables, and deployment
`dpl_HWDHVH69jkyeTqRWq2D1j2VeEdAB` reached READY. After a stale response showed
the same digest again, commit `783d505` removed the compound `Table.*` exports
from all three server-fed client table components. TypeScript and the full
38-route production build passed, and Vercel reported the commit's production
deployment complete at 2026-07-31 23:33:57 UTC.

[x] Step 5: Run the ObjectTrack2 production migration dry-run
  * Confirm exactly the six Phase 1-6 migrations are pending
  * Confirm no seed or role files will be applied

[x] Step 6: Apply and verify the ObjectTrack2 production migrations
  * Verify the remote migration history is current
  * Verify the tenant-management tables and RPCs exist
  * Keep production data intact; do not reset or seed production

[x] Step 7: Deploy and verify the updated application
  * Push the verified tenant-management application changes
  * Confirm the Vercel production build reaches READY
  * Smoke-test the deployed application and inspect runtime errors

[x] Step 8: Activate a platform operator with TOTP MFA
  * Grant only the explicitly identified internal account
  * Require the operator to enroll and verify TOTP at /mfa
  * Confirm /ops requires an AAL2 session
  * Platform-operator access granted to `bpzmik@163.com`, the sole production
    user, legacy administrator, and tenant owner
  * TOTP factor verified and the authenticated `/ops` request passed the
    operator and AAL2 checks
